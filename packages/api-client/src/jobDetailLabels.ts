/** View-mode placeholders for incomplete job detail fields (capture now, refine later). */
export const JOB_DETAIL_EMPTY_LABELS = {
  sessionDate: 'No Session Date',
  sessionDuration: 'No duration',
  materialDescription: 'No Material description',
  otherCostType: 'No Cost Type',
} as const;
