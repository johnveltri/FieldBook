# Hosted Supabase (same project as apps/mobile-expo)
# Dashboard → Settings → API → Project URL + anon key (browser) + service_role key (server)
SUPABASE_URL=https://gfvqmxsiuhhujnckghpa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=https://gfvqmxsiuhhujnckghpa.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional override for staging or a non-default Edge Function deployment.
# Defaults to `${NEXT_PUBLIC_SUPABASE_URL}/functions/v1/redeem-job-export`.
NEXT_PUBLIC_SUPABASE_REDEEM_JOB_EXPORT_URL=
WAITLIST_RATE_LIMIT_SALT=

# Local dev (optional): `npm run db:start` then `npx supabase status --workdir backend`
# SUPABASE_URL=http://127.0.0.1:54321
